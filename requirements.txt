# ── Основа: интерфейс, звук, Gemini Live ──────────────────────────────────────
PyQt6
sounddevice
numpy
google-genai>=2.8.0

# ── Поиск в сети и управление браузером ───────────────────────────────────────
requests
beautifulsoup4
duckduckgo-search
# Playwright 1.58 убрал сборки chromium/webkit для macOS 13 и старше
# (Darwin kernel < 23) — `playwright install chromium` там падает с
# "Playwright does not support chromium on mac13". 1.57.0 — последняя версия,
# где эти сборки ещё есть, поэтому на старых macOS ставим только её.
playwright; sys_platform != "darwin"
playwright<1.58; sys_platform == "darwin" and platform_release < "23"
playwright; sys_platform == "darwin" and platform_release >= "23"

# ── Автоматизация / управление вводом ─────────────────────────────────────────
pyautogui
pyperclip
pygetwindow

# ── Компьютерное зрение и медиа ───────────────────────────────────────────────
pillow
opencv-python
mss

# ── Система, файлы и документы ────────────────────────────────────────────────
psutil
send2trash
youtube-transcript-api
python-pptx
openpyxl

# ── Удалённый дашборд (управление с телефона) ─────────────────────────────────
fastapi
uvicorn[standard]
cryptography
python-multipart
qrcode[pil]

# ── Дополнения плагинов: Gmail / Google Календарь (OAuth) ─────────────────────
google-api-python-client
google-auth-oauthlib

# ── Дополнения плагинов: Tuya / Smart Life (умный свет, home_assistant) ───────
tinytuya

# ── Только Windows (pip пропускает их на macOS / Linux автоматически) ─────────
comtypes;   sys_platform == "win32"
pycaw;      sys_platform == "win32"
win10toast; sys_platform == "win32"
pywinauto;  sys_platform == "win32"
pywin32;    sys_platform == "win32"
wmi;        sys_platform == "win32"

# ── Опционально: локальное слово пробуждения ("Привет Анфиса") ───────────────────
# Намеренно НЕ устанавливается по умолчанию — это включается вручную. Активируйте
# (и скачайте крошечную модель) в один клик из ⚙ → WAKE WORD в приложении, либо
# установите сами командой:  pip install openwakeword
#
# Системные действия для macOS / Linux (громкость, яркость, напоминания) вызывают
# нативные утилиты (pactl / brightnessctl / osascript / systemd), поэтому лишних
# Python-пакетов там не нужно — ничего виндовского никогда не подтягивается.
